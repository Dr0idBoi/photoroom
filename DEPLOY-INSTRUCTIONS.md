# 🚀 Инструкция по деплою после исправлений

## ✅ Что было исправлено:

1. **Prisma Schema** - добавлен `binaryTargets` для Alpine Linux
2. **package.json** - зафиксированы версии Prisma (5.22.0)
3. **docker-compose.yml** - удалена устаревшая строка `version`
4. **deploy.sh** - обновлен для использования `docker compose` вместо `docker-compose`

---

## 📦 Шаги для деплоя:

### 1️⃣ На локальной машине (Windows):

```powershell
# Добавьте и закоммитьте изменения
git add .
git commit -m "Fix: Prisma binary targets for Alpine Linux and Docker Compose v2"
git push origin master
```

### 2️⃣ На сервере:

```bash
# Подключитесь к серверу
ssh root@gwbyjajiix

# Перейдите в проект
cd /opt/photoroom/photoroom

# Подтяните изменения
git pull origin master

# Остановите и удалите старые контейнеры
docker compose down

# Удалите старую базу данных (она пересоздастся с seed данными)
rm -f data/production.db
rm -f data/production.db-journal

# Удалите старый образ для полной пересборки
docker rmi photoroom-photoroom 2>/dev/null || true

# Пересоберите образ (это займет несколько минут)
docker compose build --no-cache

# Запустите контейнеры
docker compose up -d

# Проверьте логи (Ctrl+C для выхода)
docker compose logs -f photoroom
```

### 3️⃣ Что должно быть в логах:

✅ **Успешный запуск выглядит так:**
```
✓ Generated Prisma Client (5.22.0) in XXXms
Datasource "db": SQLite database at "file:/app/data/production.db"

Running seed command `ts-node --compiler-options {...} prisma/seed.ts` ...
🌱  The seed command has been executed.

▲ Next.js 14.2.15
- Local:   http://localhost:3000
✓ Ready in XXXms
```

---

## 🔍 Проверка работы:

### Откройте в браузере:
- **Главная:** `http://ваш_ip:3000`
- **Услуги:** `http://ваш_ip:3000/service-page-3`
- **Админка:** `http://ваш_ip:3000/admin/login`

### Войдите в админку:
- Email: `admin@getmodels.local`
- Пароль: `admin123`

### Проверьте наличие данных:
- ✅ Категории (16 шт)
- ✅ Модели (2-3 примера)
- ✅ Услуги (25 услуг)
- ✅ Блог (2 поста)
- ✅ Портфолио (1 пример)

---

## 🔧 Если что-то пошло не так:

### Проблема: Prisma ошибка с Query Engine
```bash
# Решение: Пересоберите с нуля
docker compose down
docker compose build --no-cache
docker compose up -d
```

### Проблема: База пустая (нет категорий/моделей)
```bash
# Решение: Выполните seed вручную
docker compose exec photoroom npx prisma db seed
docker compose restart photoroom
```

### Проблема: Порт 3000 занят
```bash
# Проверьте что занимает порт
sudo lsof -i :3000

# Остановите процесс или измените порт в docker-compose.yml
```

---

## 📊 Полезные команды:

```bash
# Логи в реальном времени
docker compose logs -f photoroom

# Последние 100 строк логов
docker compose logs --tail=100 photoroom

# Статус контейнеров
docker compose ps

# Перезапуск
docker compose restart photoroom

# Остановка всего
docker compose down

# Использование ресурсов
docker stats photoroom_app
```

---

## 🎯 Следующие шаги после успешного деплоя:

1. ✅ Зайдите в админку
2. ✅ Смените пароль администратора
3. ✅ Загрузите свои модели
4. ✅ Загрузите свои изображения
5. ✅ Отредактируйте контент под себя
6. ✅ Настройте SSL если нужен HTTPS (см. DEPLOYMENT.md)

---

**Удачи! 🚀**

