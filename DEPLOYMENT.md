# 🚀 Руководство по деплою Photoroom

## Требования к серверу

- **ОС:** Ubuntu 20.04+ / Debian 11+ / CentOS 8+
- **RAM:** Минимум 2GB (рекомендуется 4GB+)
- **Диск:** Минимум 10GB свободного места
- **Docker:** v20.10+
- **Docker Compose:** v2.0+
- **Git:** Любая версия

---

## 📋 Шаг 1: Подготовка сервера

### Установка Docker

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавляем пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиниваемся или выполняем
newgrp docker

# Проверяем установку
docker --version
docker compose version
```

### Установка Git (если не установлен)

```bash
sudo apt install git -y
```

---

## 📦 Шаг 2: Клонирование проекта

### Вариант A: Через HTTPS

```bash
# Переходим в директорию для проектов
cd /var/www  # или любую другую директорию

# Клонируем репозиторий
git clone https://github.com/ваш_username/photoroom-clone.git
cd photoroom-clone
```

### Вариант B: Через SSH (рекомендуется)

```bash
# Генерируем SSH ключ на сервере
ssh-keygen -t ed25519 -C "your_email@example.com"

# Добавляем ключ в GitHub
cat ~/.ssh/id_ed25519.pub
# Скопируйте вывод и добавьте в GitHub Settings > SSH Keys

# Клонируем репозиторий
git clone git@github.com:ваш_username/photoroom-clone.git
cd photoroom-clone
```

---

## ⚙️ Шаг 3: Настройка конфигурации

### Создаем необходимые директории

```bash
mkdir -p data
mkdir -p public/images/uploads
mkdir -p nginx/ssl

# Даем права на запись
chmod 755 data
chmod 755 public/images/uploads
```

### Настраиваем переменные окружения

```bash
# Создаем .env файл
cp .env.production .env

# Редактируем при необходимости
nano .env
```

### Настраиваем Next.js для standalone режима

Убедитесь что в `next.config.js` есть:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // ... остальные настройки
}

module.exports = nextConfig
```

---

## 🐳 Шаг 4: Деплой с Docker

### Вариант 1: Простой деплой (только приложение)

```bash
# Используем упрощенный docker-compose
docker compose -f docker-compose.simple.yml build
docker compose -f docker-compose.simple.yml up -d

# Проверяем статус
docker compose -f docker-compose.simple.yml ps

# Смотрим логи
docker compose -f docker-compose.simple.yml logs -f
```

Приложение будет доступно на `http://your-server-ip:3000`

### Вариант 2: С Nginx (рекомендуется для продакшна)

```bash
# Редактируем nginx конфигурацию
nano nginx/nginx.conf
# Замените "ваш_домен.ru" на ваш реальный домен

# Собираем и запускаем
docker compose build
docker compose up -d

# Проверяем
docker compose ps
docker compose logs -f
```

---

## 🔄 Шаг 5: Автоматический деплой через скрипт

### Делаем скрипт исполняемым

```bash
chmod +x deploy.sh
```

### Запускаем деплой

```bash
./deploy.sh
```

Скрипт автоматически:
1. ✅ Остановит старые контейнеры
2. ✅ Подтянет изменения из Git
3. ✅ Соберет новый Docker образ
4. ✅ Запустит контейнеры
5. ✅ Выполнит миграции базы данных
6. ✅ Покажет статус и логи

---

## 🔐 Шаг 6: Настройка SSL (HTTPS)

### Вариант A: Let's Encrypt (бесплатный SSL)

```bash
# Устанавливаем Certbot
sudo apt install certbot -y

# Получаем сертификат
sudo certbot certonly --standalone -d ваш_домен.ru -d www.ваш_домен.ru

# Копируем сертификаты
sudo cp /etc/letsencrypt/live/ваш_домен.ru/fullchain.pem nginx/ssl/
sudo cp /etc/letsencrypt/live/ваш_домен.ru/privkey.pem nginx/ssl/

# Даем права
sudo chmod 644 nginx/ssl/*.pem

# Раскомментируйте SSL строки в nginx/nginx.conf
nano nginx/nginx.conf

# Перезапускаем Nginx
docker compose restart nginx
```

### Автоматическое обновление сертификата

```bash
# Добавляем в crontab
sudo crontab -e

# Добавьте строку:
0 0 1 * * certbot renew --quiet && cp /etc/letsencrypt/live/ваш_домен.ru/*.pem /path/to/project/nginx/ssl/ && docker compose restart nginx
```

---

## 📊 Полезные команды

### Управление контейнерами

```bash
# Просмотр логов
docker compose logs -f photoroom
docker compose logs -f nginx

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Полная пересборка
docker compose down
docker compose build --no-cache
docker compose up -d

# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats
```

### Работа с базой данных

```bash
# Выполнить миграции вручную
docker compose exec photoroom npx prisma migrate deploy

# Пересоздать данные (ОСТОРОЖНО! Удалит все данные)
docker compose exec photoroom npx prisma db push --force-reset
docker compose exec photoroom npx prisma db seed

# Бэкап базы данных
cp data/production.db data/production.db.backup-$(date +%Y%m%d)
```

### Git workflow

```bash
# На локальной машине - пушим изменения
git add .
git commit -m "Update: описание изменений"
git push origin main

# На сервере - деплоим
./deploy.sh
```

---

## 🔍 Диагностика проблем

### Проверка логов

```bash
# Логи приложения
docker compose logs -f photoroom

# Последние 100 строк
docker compose logs --tail=100 photoroom

# Логи Nginx
docker compose logs -f nginx
```

### Проверка портов

```bash
# Проверяем что порт 3000 слушается
sudo netstat -tulpn | grep 3000

# Проверяем что порт 80/443 слушается
sudo netstat -tulpn | grep -E '80|443'
```

### Проверка файловой системы

```bash
# Проверяем права на директории
ls -la data/
ls -la public/images/uploads/

# Проверяем содержимое
ls -lh data/
```

### Решение типичных проблем

**Проблема:** База данных не создается
```bash
# Решение: создаем вручную
docker compose exec photoroom npx prisma migrate deploy
docker compose exec photoroom npx prisma db seed
```

**Проблема:** Нет доступа к загруженным изображениям
```bash
# Решение: проверяем volume
docker compose down
chmod -R 755 public/images/uploads/
docker compose up -d
```

**Проблема:** Порт 3000 занят
```bash
# Решение: меняем порт в docker-compose.yml
# ports:
#   - "3001:3000"  # вместо 3000:3000
```

---

## 🔄 Обновление приложения

### Стандартное обновление

```bash
# Просто запустите скрипт деплоя
./deploy.sh
```

### Ручное обновление

```bash
# 1. Подтягиваем изменения
git pull origin main

# 2. Пересобираем образ
docker compose build

# 3. Перезапускаем с обновлением
docker compose up -d

# 4. Проверяем
docker compose logs -f photoroom
```

---

## 🎯 Продакшн чеклист

Перед запуском в продакшн убедитесь:

- ✅ Установлен SSL сертификат
- ✅ Настроен firewall (открыты только нужные порты)
- ✅ Включен автоматический перезапуск контейнеров (`restart: unless-stopped`)
- ✅ Настроен мониторинг (логи, метрики)
- ✅ Настроен автоматический бэкап базы данных
- ✅ Изменен дефолтный пароль админа
- ✅ Настроены переменные окружения в `.env`
- ✅ Проверена работа загрузки файлов
- ✅ Настроен домен и DNS
- ✅ Проверена работа всех функций админки

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker compose logs -f`
2. Убедитесь что все порты открыты: `sudo ufw status`
3. Проверьте права на файлы: `ls -la data/`
4. Попробуйте полную пересборку: `docker compose down && docker compose build --no-cache && docker compose up -d`

---

## 📝 Краткая шпаргалка

```bash
# Первоначальная установка
git clone <repo>
cd photoroom-clone
chmod +x deploy.sh
./deploy.sh

# Обновление
git pull
./deploy.sh

# Логи
docker compose logs -f photoroom

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Бэкап БД
cp data/production.db data/backup-$(date +%Y%m%d).db
```

Удачного деплоя! 🚀

