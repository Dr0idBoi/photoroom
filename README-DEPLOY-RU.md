# 🚀 Деплой Photoroom - Пошаговая инструкция

## 📦 Что вы получите

После деплоя у вас будет:
- ✅ Полностью рабочий сайт модельного агентства
- ✅ Админ-панель для управления контентом
- ✅ База данных SQLite с примерами
- ✅ Автоматические миграции
- ✅ Загрузка изображений
- ✅ Docker контейнеризация
- ✅ Nginx reverse proxy (опционально)

---

## 🎯 Вариант 1: Самый быстрый способ (5 минут)

### На вашем компьютере:

1. **Подготовьте проект для Git:**
```bash
# Убедитесь что вы в папке проекта
cd Clone

# Инициализируем Git (если еще не сделано)
git init
git add .
git commit -m "Initial commit: Photoroom modeling agency"
```

2. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Нажмите "New repository"
   - Назовите репозиторий (например, `photoroom`)
   - НЕ добавляйте README, .gitignore или лицензию
   - Нажмите "Create repository"

3. **Загрузите код на GitHub:**
```bash
# Замените YOUR_USERNAME на ваш username GitHub
git remote add origin https://github.com/YOUR_USERNAME/photoroom.git
git branch -M main
git push -u origin main
```

### На сервере:

1. **Подключитесь к серверу:**
```bash
ssh user@your-server-ip
```

2. **Установите Docker (если не установлен):**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
newgrp docker
```

3. **Клонируйте и запустите проект:**
```bash
# Переходим в директорию для проектов
cd /var/www  # или любую другую

# Клонируем репозиторий (замените YOUR_USERNAME)
git clone https://github.com/YOUR_USERNAME/photoroom.git
cd photoroom

# Создаем нужные директории
mkdir -p data public/images/uploads nginx/ssl
chmod 755 data public/images/uploads

# Делаем скрипт деплоя исполняемым
chmod +x deploy.sh

# ЗАПУСКАЕМ ДЕПЛОЙ!
./deploy.sh
```

4. **Готово!** 🎉

Ваш сайт доступен на: `http://ваш-ip-сервера:3000`

Админ-панель: `http://ваш-ip-сервера:3000/admin/login`
- Email: `admin@getmodels.local`
- Пароль: `admin123`

---

## 🎯 Вариант 2: Без Nginx (только приложение)

Если вам не нужен Nginx, используйте упрощенную версию:

```bash
# На сервере после клонирования
docker compose -f docker-compose.simple.yml build
docker compose -f docker-compose.simple.yml up -d

# Проверяем
docker compose -f docker-compose.simple.yml logs -f
```

---

## 🎯 Вариант 3: С Nginx и SSL

### 1. Настройте домен
Добавьте A-запись в DNS вашего домена, указывающую на IP сервера.

### 2. Отредактируйте Nginx конфиг
```bash
nano nginx/nginx.conf
# Замените "ваш_домен.ru" на ваш реальный домен
```

### 3. Получите SSL сертификат
```bash
# Устанавливаем Certbot
sudo apt install certbot -y

# Временно останавливаем контейнеры
docker compose down

# Получаем сертификат
sudo certbot certonly --standalone -d ваш_домен.ru -d www.ваш_домен.ru

# Копируем сертификаты
sudo cp /etc/letsencrypt/live/ваш_домен.ru/fullchain.pem nginx/ssl/
sudo cp /etc/letsencrypt/live/ваш_домен.ru/privkey.pem nginx/ssl/
sudo chmod 644 nginx/ssl/*.pem
```

### 4. Раскомментируйте SSL в Nginx
```bash
nano nginx/nginx.conf
# Раскомментируйте строки с ssl_certificate
```

### 5. Запустите с Nginx
```bash
docker compose up -d
```

Теперь сайт доступен на: `https://ваш_домен.ru`

---

## 🔄 Как обновить сайт после изменений

### На вашем компьютере:
```bash
# Делаете изменения в коде, затем:
git add .
git commit -m "Описание изменений"
git push origin main
```

### На сервере:
```bash
cd /var/www/photoroom
./deploy.sh
```

Скрипт автоматически:
1. Подтянет изменения из Git
2. Пересоберет Docker образ
3. Перезапустит контейнеры
4. Выполнит миграции БД

---

## 📊 Полезные команды

### Просмотр логов
```bash
# Логи приложения
docker compose logs -f photoroom

# Последние 100 строк
docker compose logs --tail=100 photoroom

# Логи Nginx (если используете)
docker compose logs -f nginx
```

### Управление контейнерами
```bash
# Статус
docker compose ps

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Полная пересборка
docker compose down
docker compose build --no-cache
docker compose up -d
```

### Работа с базой данных
```bash
# Бэкап БД
cp data/production.db data/backup-$(date +%Y%m%d-%H%M%S).db

# Восстановление из бэкапа
cp data/backup-YYYYMMDD-HHMMSS.db data/production.db
docker compose restart
```

### Работа с изображениями
```bash
# Просмотр загруженных изображений
ls -lh public/images/uploads/

# Очистка старых изображений (ОСТОРОЖНО!)
# rm public/images/uploads/старое_изображение.jpg
```

---

## 🔧 Решение проблем

### Проблема: "Cannot connect to Docker daemon"
```bash
# Решение:
sudo systemctl start docker
sudo usermod -aG docker $USER
newgrp docker
```

### Проблема: "Port 3000 is already in use"
```bash
# Решение 1: Остановите другие приложения на порту 3000
sudo lsof -i :3000
sudo kill -9 <PID>

# Решение 2: Измените порт в docker-compose.yml
# ports:
#   - "3001:3000"
```

### Проблема: "Permission denied" при работе с файлами
```bash
# Решение:
sudo chown -R $USER:$USER /var/www/photoroom
chmod -R 755 /var/www/photoroom/data
chmod -R 755 /var/www/photoroom/public/images/uploads
```

### Проблема: База данных не создается
```bash
# Решение:
docker compose exec photoroom npx prisma migrate deploy
docker compose exec photoroom npx prisma db seed
docker compose restart
```

### Проблема: Изображения не загружаются
```bash
# Решение:
chmod -R 755 public/images/uploads/
docker compose restart
```

---

## 🔐 Безопасность

### После деплоя обязательно:

1. **Измените пароль админа:**
   - Зайдите в админку
   - Создайте нового админа с безопасным паролем
   - Удалите дефолтного админа

2. **Настройте файрвол:**
```bash
sudo ufw allow 22    # SSH
sudo ufw allow 80    # HTTP
sudo ufw allow 443   # HTTPS
sudo ufw enable
```

3. **Регулярные бэкапы:**
```bash
# Добавьте в crontab
crontab -e

# Добавьте строку (бэкап каждый день в 3 ночи):
0 3 * * * cp /var/www/photoroom/data/production.db /var/www/photoroom/data/backup-$(date +\%Y\%m\%d).db
```

---

## 📈 Мониторинг

### Проверка здоровья приложения
```bash
# Проверка что приложение отвечает
curl http://localhost:3000

# Проверка использования ресурсов
docker stats

# Проверка дискового пространства
df -h
```

### Настройка алертов (опционально)
Используйте сервисы типа:
- UptimeRobot (бесплатный мониторинг доступности)
- Sentry (отслеживание ошибок)
- Grafana + Prometheus (продвинутый мониторинг)

---

## 🎓 Дополнительные ресурсы

- **Полная документация:** `DEPLOYMENT.md`
- **Быстрый старт:** `QUICK-DEPLOY.md`
- **Руководство по админке:** `ADMIN-GUIDE.md`
- **Документация Docker:** https://docs.docker.com
- **Документация Next.js:** https://nextjs.org/docs

---

## ✅ Чеклист перед продакшном

- [ ] SSL сертификат установлен
- [ ] Домен настроен и работает
- [ ] Файрвол настроен
- [ ] Пароль админа изменен
- [ ] Настроены автоматические бэкапы
- [ ] Проверена загрузка изображений
- [ ] Проверены все страницы сайта
- [ ] Проверена админ-панель (создание/редактирование/удаление)
- [ ] Настроен мониторинг
- [ ] Документированы учетные данные

---

## 🆘 Нужна помощь?

1. Проверьте логи: `docker compose logs -f`
2. Прочитайте `DEPLOYMENT.md` для подробной информации
3. Проверьте что все порты открыты: `sudo ufw status`
4. Убедитесь что Docker запущен: `docker ps`

---

**Удачного деплоя! 🚀**

Если все работает, не забудьте:
- ⭐ Поставить звезду репозиторию на GitHub
- 📸 Загрузить свои фотографии моделей
- ✍️ Написать свой контент
- 🎨 Настроить дизайн под себя

